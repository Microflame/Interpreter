// old impl finishes fib(40) in 220 s.
// reference is said to finish in 72 s.

func fib(n) {
  if (n < 2) return n;
  return fib(n - 1) + fib(n - 2); 
}

var before = clock();
print(fib(40));
var after = clock();
print(after - before);
